import "./style.css";

import { createRoot } from "react-dom/client";

const IndexPage = () => <h1 style={{ padding: 20 }}>work in progress</h1>;

const root = createRoot(document.getElementById(".bungie-api-webhooks"));
root.render(<IndexPage />);
