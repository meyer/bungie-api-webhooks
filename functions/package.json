{
  "name": "bungie-api-webhooks-functions",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "engines": {
    "node": "16"
  },
  "scripts": {
    "build": "rm -rf lib/* && tsc",
    "build:watch": "tsc --watch",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "@google-cloud/pubsub": "^3.2.1",
    "bungie-api-ts": "^4.21.0",
    "node-fetch": "^3.3.0"
  }
}
